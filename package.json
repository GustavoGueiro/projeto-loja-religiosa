{
  "name": "api-loja-religiosa",
  "version": "1.0.0",
  "description": "API da Loja Religiosa com JSON Server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT --static ./public"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
