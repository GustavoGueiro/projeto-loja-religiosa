{
  "name": "projetos-loja-religiosa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port 8080",
    "start:local": "json-server --watch db.json --host 0.0.0.0 --port 8080",
    "start:render": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
